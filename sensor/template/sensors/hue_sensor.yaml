utebod_sensor:
  friendly_name: 'Sensor'
  value_template: >-
    {% if is_state('sensor.utebod_motion_sensor', 'on') %}
      Bevegelse registrert
    {% else %}
      Ingen bevegelse registrert
    {% endif %}

gangen_sensor:
  friendly_name: 'Sensor'
  value_template: >-
    {% if is_state('sensor.gangen_motion_sensor', 'on') %}
      Bevegelse registrert
    {% else %}
      Ingen bevegelse registrert
    {% endif %}

boden_sensor:
  friendly_name: 'Sensor'
  value_template: >-
    {% if is_state('sensor.boden_motion_sensor', 'on') %}
      Bevegelse registrert
    {% else %}
      Ingen bevegelse registrert
    {% endif %}

soverommet_sensor:
  friendly_name: 'Sensor'
  value_template: >-
    {% if is_state('sensor.soverommet_motion_sensor', 'on') %}
      Bevegelse registrert
    {% else %}
      Ingen bevegelse registrert
    {% endif %}